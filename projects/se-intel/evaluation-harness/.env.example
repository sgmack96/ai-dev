# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Live Worker URL
BASE_URL=https://se-intel-portfolio.stephenmack96.workers.dev

# Cloudflare account ID (not a secret — appears in dashboard URLs)
CF_ACCOUNT_ID=7935a6f1039d4c27e307c443d452d6bf

# Cloudflare API token — needs only: Account > Workers AI > Edit
# Create at: dash.cloudflare.com > My Profile > API Tokens
CF_API_TOKEN=your-token-here
